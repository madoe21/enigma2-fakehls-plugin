# Line endings: shell scripts and hooks must stay LF — CRLF breaks bash on
# Windows checkouts ("$'': command not found").
* text=auto
*.sh text eol=lf
*.py text eol=lf
*.json text
*.md text
*.bat text eol=crlf
